# Required by Cloudflare Pages CI during deployment (CF Pages runs npm install
# without allowing per-command flags). No active peer conflicts exist as of the
# last audit; re-check when upgrading major dependencies (e.g. vite, react, gsap).
legacy-peer-deps=true
